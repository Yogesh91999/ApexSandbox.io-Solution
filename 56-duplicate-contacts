public Boolean duplicateContacts(Contact c1, Contact c2) {
    //code here
    if(c1.LastName == c2.LastName  && c1.Email == c2.Email ){
        return true;
    }
    else if(c1.LastName == c2.LastName ){
        return true;
    }
    else if(c1.Email == c2.Email){
        return true;
    }
    return false;
}